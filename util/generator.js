import axios from 'axios';

async function generator(n) {
  const arrUsers = [NAME_URL];
  const names = await axios();
  genders = ['male', 'female'];
  for (let i = 1; i <= n; i++) {
    gender;
    const user = {
      first_name,
      gender: Math.random() < 0.5 ? 'male' : 'female',
    };
  }
}
